{
  "name": "coreutilsjs",
  "version": "0.1.0",
  "description": "Reimplementing some standard unix commands in JS for cross platform use",
  "main": "build/index.js",
  "author": "dk949",
  "license": "MIT",
  "scripts": {
    "build": "yarn tsc",
    "cat": "yarn build && node build/cat.js",
    "echo": "yarn build && node build/echo.js",
    "false": "yarn build && node build/false.js",
    "index": "yarn build && node build/index.js",
    "rm": "yarn build && node build/rm.js",
    "true": "yarn build && node buildtrue.js/"
  },
  "devDependencies": {
    "@types/node": "^17.0.5",
    "@typescript-eslint/eslint-plugin": "^5.4.0",
    "@typescript-eslint/parser": "^5.4.0",
    "eslint": "^8.2.0",
    "typescript": "^4.5.2"
  }
}
